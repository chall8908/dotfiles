;;; elixir.el --- elixir mode -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package elixir-mode)

(use-package flycheck-elixir
  :after (flycheck)
  )

(provide 'elixir)

;;; elixir.el ends here
